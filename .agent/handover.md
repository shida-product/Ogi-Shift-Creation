# 引継ぎメモ

## 現在地
シフト再生成後に「生成からの差分」マークが残る不具合を修正済み（未コミット）。
生成完了時は Reset 相当のクリーン状態にし、同期が古いスナップショットで上書きしないようガード。

## 次の行動
- 生成画面で再生成し、差分枠・手動点・差分パネルが消えることを確認
- 問題なければコミット（指示待ち）

## 外部状態
- 本番 Supabase の `ogi_shift_requests.change_history` カラムは追加済み。

## キャッシュ
- `generate.html` JS `generate.js?v=35`
