# 引継ぎメモ（最終更新: 2026-08-23）

## 現在地
生成画面の表示を DB 正に統一し、タブ復帰時の他 PC 同期までコミット・push 済み。

## 次の行動
- 特になし

## 外部状態
- 本番 Supabase の `ogi_shift_requests.change_history` カラムは追加済み。

## キャッシュ
- `generate.html` CSS `generate.css?v=14` / JS `generate.js?v=34`
